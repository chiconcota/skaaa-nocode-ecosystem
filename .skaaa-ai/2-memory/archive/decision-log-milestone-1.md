# DECISION LOG - ARCHIVE MILESTONE 1 (PHASE 1, 2, 3 & 4)
@status: ARCHIVED | @context: Lịch sử quyết định kiến trúc cốt lõi từ 2026-05-24 đến 2026-08-21
---

## 2026-08-21 - 🟢 Hoàn thành: Hotfix v2.0.1 - JIT Editor Recursion, Clean Block Markup & Source Table Separation
- **Decision (Clean Dynamic Block Markup Protocol - Skaaa No-Code Design v2.3.2):**
  - **Vấn đề:** Khi chèn thẻ HTML tĩnh thô (`<main>`, `<div>`, `<h1>`, `<p>`) lồng bên trong comment block Gutenberg cho các khối Dynamic / ServerSideRendered block (Container, Loop, Text, Button), cơ chế kiểm tra tính hợp lệ của Gutenberg (Block Validation Parser) báo lỗi *"Block contains unexpected or invalid content"*.
  - **Quyết định:** Chuẩn hóa toàn bộ cấu trúc markup: `skaaaaa-builder/container` chỉ chứa inner blocks (thẻ HTML wrapper do `render.php` sinh động), các khối `text` và `loop` là dạng tự đóng `<!-- wp:... {...} /-->`.
- **Decision (Source Table Separation & Toggle Control - Skaaa No-Code Design v2.3.2):**
  - **Vấn đề:** Dropdown *Source Table* của khối `Skaaa Loop` và `Skaaa Select` liệt kê cả các bảng hệ thống nội bộ (`skaaa_data_sys_tokens`, `sys_apps`, `sys_workflows`, `sys_settings`, `sys_organisms`, `sys_theme_templates`), gây rối mắt cho người dùng và tiềm ẩn rủi ro lộ dữ liệu cấu hình ra ngoài frontend.
  - **Quyết định:** Mặc định chỉ hiển thị các bảng dữ liệu nghiệp vụ của ứng dụng (`📦 [App Data]`). Thêm `ToggleControl` *"Show System Tables (Internal)"* cho phép Super Admin chủ động hiển thị các bảng hệ thống (`⚙️ [System]`) khi thực sự cần thiết.
- **Decision (E2E Automated Test Suite & Multi-Scenario Verification):**
  - Xây dựng bộ test runner tự động `e2e_test_suite.php` kiểm thử 19 assertions cho 3 kịch bản: Complex Glassmorphism Templates, Loop Slots with Mustache Dynamic Binding, và Dedicated Portal Routing App. Toàn bộ 19/19 test cases đạt 100% PASS.

## 2026-08-16 - 🟢 Hoàn thành: Khắc phục Schema Bảng Hệ Thống & Tối ưu JIT Editor cho Khối Skaaa Symbol Reference
- **Decision (Atomic dbDelta System Tables Schema Creation - Skaaa Data Pro v1.3.3):**
  - **Vấn đề:** Trên các môi trường cài đặt mới hoặc vừa di chuyển hệ thống, hàm `maybe_create_system_tables()` sử dụng phương pháp `create_custom_table` + `add_column()`. Tuy nhiên, vì các bảng hệ thống (`wp_skaaa_data_sys_*`) được bảo vệ bởi cơ chế `is_table_protected()`, các lệnh `add_column()` bị chặn đứng hoàn toàn. Kết quả là bảng `wp_skaaa_data_sys_organisms` và `wp_skaaa_data_sys_theme_templates` bị thiếu các cột nghiệp vụ (`name`, `type`, `json_content`, `html_content`, `category`, `location`, `organism_id`, `conditions`, `is_active`), khiến tính năng tạo và lưu Organisms/Symbols bị lỗi crash SQL `Unknown column 'name' in 'field list'` (HTTP 500).
  - **Quyết định:** Refactor toàn bộ `App_Manager::maybe_create_system_tables()` sang sử dụng hàm `dbDelta()` chuẩn của WordPress với định nghĩa đầy đủ 100% các cột ngay từ câu lệnh `CREATE TABLE`. Bổ sung thêm cơ chế tự động kiểm tra và thêm cột còn thiếu (self-healing column checks) cho cả 4 bảng hệ thống (`sys_organisms`, `sys_theme_templates`, `sys_presets`, `sys_apps`) để đảm bảo các môi trường hiện có và cài mới đều có cấu trúc bảng hoàn hảo.
- **Decision (Organism Reference Editor JIT Parity & Live Canvas DOM Scanner - Skaaa No-Code Design v2.3.1):**
  - **Vấn đề:** Khi chuyển từ Tailwind CDN sang bộ biên dịch offline `SkaaaWindCompiler`, khối `Skaaa Symbol Reference` (`skaaaaa-builder/organism-ref`) trong Gutenberg Editor hiển thị chữ thô không nhận style CSS (mặc dù ngoài Frontend vẫn nhận bình thường). Lý do: Block Symbol chỉ lưu thuộc tính `{ organismId: "5" }`, còn toàn bộ nội dung HTML được render qua `ServerSideRender`. Bộ biên dịch `runJITCompilation()` chỉ quét cây block của Gutenberg (`getBlocks()`) nên bị sót toàn bộ các class Tailwind nằm bên trong Symbol.
  - **Quyết định:** Nâng cấp `runJITCompilation()` trong `skaaa-editor-helper.js`:
    1. Khi duyệt cây block, nếu gặp `skaaaaa-builder/organism-ref` hoặc `skaaaaa-builder/loop`, tự động tra cứu `window.skaaaOrganismsCache[organismId].html_content` để trích xuất toàn bộ class Tailwind bên trong Symbol.
    2. Bổ sung bộ quét Live Canvas DOM (`activeIframeDoc.querySelectorAll('[class]')`) kết hợp với `MutationObserver` để tự động bắt các node DOM được chèn vào sau khi `ServerSideRender` hoàn tất và kích hoạt biên dịch CSS JIT thời gian thực.
    3. Cập nhật `class-core.php` để truyền kèm `html_content` vào `window.skaaaOrganismsCache` khi tải Editor.

## 2026-07-22 - 🟢 Hoàn thành: Sửa lỗi Vertical Grid Height Alignment, Loại bỏ !important & Auto-seed Design Tokens DB
- **Decision (Frontend Grid Height Alignment):** Thêm điều kiện lọc `:not([class*="grid"]):not([class*="flex"])` vào quy tắc `.skaaa-container-block > * + *` trong `class-tailwind-config.php`, triệt tiêu thuộc tính `margin-top: 24px` bị gán nhầm cho phần tử con trong Flex/Grid, giúp các cột hiển thị phẳng 100% trên cùng đường kẻ ngang cả Editor lẫn Frontend.
- **Decision (Zero !important Specificity Policy):** Refactor toàn bộ `skaaa-editor-helper.js` loại bỏ 100% cờ `!important` dư thừa. Sử dụng bộ gom nhóm Specificity Scope `.editor-styles-wrapper.editor-styles-wrapper` để đè style mặc định của WP Admin theo đúng Test Case 5.
- **Decision (Sys Presets DB Schema & Auto-Seeding):** Bổ sung migration cột `name VARCHAR(255)` vào bảng `wp_skaaa_data_sys_presets`, tích hợp tự động seed 48 bản ghi tiêu chuẩn và xuất `tokens.json` khi database trống, giải quyết triệt để lỗi rơi vào fallback sai màu trên bài viết Frontend. Nâng phiên bản `Skaaa No-Code Design` lên `v2.3.0`.

## 2026-07-22 - 🟢 Hoàn thành: Phase 6: Thiết lập Single Source of Truth qua JSON (tailwind-rules.json)
- **Decision (Single Source of Truth via tailwind-rules.json):** Tập trung toàn bộ quy tắc biên dịch tĩnh của Tailwind CSS (media queries, basic colors, weights, shadow map, max-width, margins, layout grid, size map, typography maps, border/ring/gradient/radius/blur maps và 22 palette màu) vào tệp JSON duy nhất `tailwind-rules.json`.
- **Decision (PHP & JS Dynamic JSON Rule Injection):** `Tailwind_Config` phía PHP và `SkaaaWindCompiler` phía Javascript đều nạp quy tắc động từ `tailwind-rules.json` (thông qua `wp_localize_script('skaaa-editor-helper', 'skaaaEditorConfig', ['tailwindRules' => ...])`). Điều này đảm bảo 100% Compiler Parity và tách biệt tuyệt đối giữa từ điển tiêu chuẩn Tailwind với các Design System Tokens tĩnh/động từ Theme Options (`tokens.json`).

## 2026-07-22 - 🟢 Hoàn thành: Khắc phục triệt để bóp méo Editor Canvas Layout, loại bỏ !important & Fix đính dấu # URL
- **Decision (Editor Selector Scope & CSS Specificity):** Chuyển đổi `editorBaseSelector` từ dạng chuỗi CSV dễ bị trượt vế sang `:where(.editor-styles-wrapper)`. Đồng thời loại bỏ toàn bộ cờ `!important` trong override layout bằng cách tăng độ ưu tiên CSS tự nhiên (`.editor-styles-wrapper.editor-styles-wrapper` và `html body.skaaaaa-builder .editor-styles-wrapper`).
- **Decision (Skaaapine Wrapper Display Contents):** Xử lý wrapper trung gian `.skaaapine-wrapper { display: contents; }` trong Editor Canvas để trả các phần tử con `md:col-span-*` về làm con trực tiếp của `.grid`, khôi phục 100% tỷ lệ layout đồng nhất với Frontend (Width = `641.5px`).
- **Decision (Hash-less Event Handler Protocol):** Áp dụng chuẩn modifier `@click.prevent` của Alpine.js cho toàn bộ nút bấm/tab/mobile menu để ngăn chặn hành vi gán `#` URL và nhảy cuộn trang lên đầu.

## 2026-07-21 - 🟢 Hoàn thành: Triển khai SkaaaWind JS JIT, tối ưu đồng bộ Iframe I/O chống xung đột bộ gõ & Hỗ trợ Arbitrary Colors
- **Decision (Gutenberg Iframe Warning Elimination):** Quyết định loại bỏ hoàn toàn việc tiêm thẻ style JIT có ID `skaaawind-compiled-css` vào trang cha (Main Document) khi chạy chế độ Iframe. Điều này ngăn Gutenberg quét thấy ID lạ từ main document và tự ý clone vào Iframe gây in cảnh báo ra console.
- **Decision (Gutenberg Iframe State Protection):** Để tránh xung đột với các bộ gõ tiếng Việt (như `fcitx5-lotus` dùng cổng `uinput` giả lập DOM input) do việc reload/ghi đè DOM/CSSOM liên tục (Style Flashing / CSSOM Reflow): 
  - Lưu trữ tham chiếu Iframe Document đang hoạt động.
  - Chỉ reset `previousHash = ""` và thực hiện biên dịch lại từ đầu **đúng một lần duy nhất** khi Iframe thực sự thay đổi document context (khi tải lại trang hoặc dựng lại Iframe).
  - Khi soạn thảo bình thường trong editor, compiler sẽ giữ nguyên CSSOM tĩnh lặng nếu class không thay đổi, bảo vệ bộ gõ hoạt động ổn định 100%.
- **Decision (Arbitrary Colors JIT Support):** Bổ sung regex hỗ trợ biên dịch mã màu tự chọn dạng hex arbitrary (`bg-[#...]`, `text-[#...]`, `border-[#...]`...) cho cả bộ PHP compiler (Frontend) và JS compiler (Gutenberg Editor) giúp đồng bộ hiển thị mà không cần tải CDN ngoài.
- **Decision (Dynamic CDN Fallback Integration):** Kích hoạt cơ chế nạp động Tailwind CDN khẩn cấp vào Iframe khi và chỉ khi phát hiện có class lạ chưa được biên dịch offline (unresolved) nhằm bảo đảm layout hiển thị tốt nhất, trong khi 98% class cơ bản vẫn hoạt động offline 100% không chạm internet.

## 2026-07-20 - 🟡 In Progress: Phê duyệt kế hoạch triển khai bộ biên dịch SkaaaWind JS (Phase 1-4)
- **Decision (Plan Approved):** Kế hoạch triển khai [implementation_plan.md](file:///home/chiconcota/.gemini/antigravity-ide/brain/e9181566-848c-4ebe-b42f-c3906a5fd260/implementation_plan.md) đã được phê duyệt, bao gồm việc xây dựng bộ biên dịch local-first, tích hợp subscriber Gutenberg `wp.data.subscribe` để quét block tree và inject stylesheet động vào Editor canvas, thay thế hoàn toàn Tailwind CDN ngoài.
- **Decision (Core JIT Decoupling):** Quyết định tách nhân biên dịch Core JIT Compiler viết bằng Vanilla JS (ES6 Class) thuần để đảm bảo tính độc lập và di động (Rule 5 Agnostic Core Libraries), có thể đóng gói npm package độc lập sau này.
- **Decision (Skaaapine Store Sync):** Thiết lập cơ chế đồng bộ hóa với Skaaapine Store. Khi Skaaapine chuyển đổi trạng thái (ví dụ: dark mode `skaaaTheme`), nó sẽ tự động thay đổi class `.dark` trên document root của canvas. Bộ biên dịch SkaaaWind JS sẽ sinh selector tương thích để Editor phản hồi tức thì mà không cần CDN ngoài.

## 2026-07-13 - 🟢 Hoàn thành: Đổi tên thương hiệu sang SKAAA, Quy hoạch Bridge & Di cư sang Local Site mới sạch rác
- **Decision (Rebranding to SKAAA)**: Quyết định đổi tên toàn bộ hệ sinh thái từ `Ska` sang `SKAAA` (đại diện cho **S**ystem Design, **K**ey Database, **a**ction, **a**i, và **a**gent/automation). Chạy script tự động đổi tên tệp/thư mục, thay thế chuỗi trong 270 file nguồn, di cư 13 bảng phẳng MySQL và cập nhật comment block trong 873 bài viết. Biên dịch lại webpack/vite cho các plugin chính thành công.
- **Decision (Strategic Pivot to Native SSR Monolith)**: Từ bỏ hoàn toàn định hướng headless React bên ngoài, tập trung tối đa sức mạnh vào mô hình Native SSR Monolith chạy trực tiếp trên WordPress core sử dụng Tailwind JIT compile PHP và Alpine.js tương tác động ở frontend, giúp đạt hiệu năng tối đa với chi phí vận hành thấp nhất.
- **Decision (Bridge Decoupling & Skaaai AI Addon)**: Khai tử plugin `skaaa-bridge` lỗi thời. Phân rã và đưa các tính năng của nó về đúng vị trí tự nhiên: `html2tailwind` về `skaaa-no-code-design`; các cổng dữ liệu tĩnh `Integration REST APIs` về `skaaa-data-pro`; và `Webhooks` về `skaaa-logic-engine`. Khởi tạo plugin mới **Skaaai** (AI Addon) làm addon decoupled 100% chuyên trách tích hợp LLM (Gemini/OpenAI) đăng ký node qua Pluggable Nodes API.
- **Decision (Clean Site Migration & Gitignore Alignment)**: Sửa đổi `.gitignore` whitelists để hỗ trợ tên thư mục `skaaa-*` mới, tránh việc Git tự động loại trừ tệp. Commit sạch sẽ và đẩy tag `v2.0.0` lên GitHub. Thực hiện khởi tạo và đồng bộ mã nguồn chính quy sang Local Site mới tinh sạch rác tại `/home/chiconcota/Local Sites/skaaa-no-code-ecosystem/app/public/` và kích hoạt tự động các package bằng WP-CLI.

## 2026-07-09 - 🟢 Hoàn thành: Triển khai Pluggable Nodes Registry API, Sidebar loading động, Dynamic Settings Panel & Direct Extensions Dashboard Manager (v1.3.0)
- **Decision (Central Node Registry):** Triển khai class `Skaaa_Node_Registry` tập trung để quản lý các node và filter `skaaa_logic_registered_nodes` cho phép bên thứ ba đăng ký nodes tùy biến từ các plugin mở rộng độc lập một cách decoupled tuyệt đối.
- **Decision (Dynamic Sidebar Loading & Lucide Mapping):** Đọc động danh sách node trên sidebar React từ JS context `window.SKAAA_DAG_CONTEXT.AVAILABLE_NODES`. Tích hợp helper ánh xạ chuỗi tên icon từ PHP (Zap, Variable, Database...) thành React Element thông qua thư viện `lucide-react` để bên thứ ba tự chọn icon mong muốn.
- **Decision (Dynamic Settings Panel Fallback):** Xây dựng component `DynamicNodeSettings` giải cấu trúc JSON Schema (`settings_schema`) để tự động vẽ form cấu hình động (input, password, textarea, select, toggle) cho các node ngoài core, đồng bộ reactive về state nodes của Canvas.
- **Decision (Direct Extensions Manager on Dashboard):** Quyết định bỏ qua việc lưu trạng thái tắt mở qua database phẳng `wp_skaaa_data_sys_settings` để tối giản hóa dữ liệu. Tận dụng trực tiếp API plugin cốt lõi của WordPress (`activate_plugins`, `deactivate_plugins`, `delete_plugins`) kết hợp với AJAX và giao diện Card toggle trực tiếp trên Skaaa System Dashboard, hỗ trợ quản trị viên bật, tắt, và xóa vật lý các addon một cách nhanh gọn, an toàn và sạch sẽ.

## 2026-07-08 - 🟡 Lên kế hoạch: Định hình kiến trúc Pluggable Nodes & Lộ trình Đóng gói Open-source
- **Decision (Pluggable Nodes Framework Priority):** Quyết định ưu tiên phát triển **Pluggable Nodes Framework (Hệ thống cắm rút Node Cộng đồng)** trong Milestone 2 trước khi thực hiện viết code cho plugin *Skaaa Video Engine*. Điều này đảm bảo toàn bộ các module tích hợp bên ngoài được viết dưới dạng các plugin độc lập 100% ngay từ đầu, loại bỏ nguy cơ bloat code và nợ kỹ thuật trong core.
- **Decision (Dynamic settings via JSON Schema):** Thống nhất thiết kế cơ chế cấu hình node thông qua **JSON Schema**. Plugin bên thứ ba chỉ cần khai báo metadata các trường cài đặt bằng PHP ở Backend, React Core ở Frontend sẽ tự động parse để sinh Form điều khiển tương ứng. Lập trình viên độc lập không cần biết React hay Webpack vẫn có thể viết node mới dễ dàng.
- **Decision (Modular Monorepo for Open-Source):** Trì hoãn việc đóng gói các lõi công nghệ (`SkaaaFX`, `Skaaapine`, `Tailwind JIT`) thành các repository độc lập mã nguồn mở (Composer/npm) cho đến khi các API này thực sự ổn định và "đóng băng". Trong lúc này, bắt buộc viết code sạch, decoupled hoàn toàn khỏi WordPress để chuẩn bị tốt cho việc cắt package sau này.

## 2026-06-25 - 🟢 Hoàn thành: Tích hợp block code lồng Container, Sửa lỗi nạp Header CSS/JS & Cải tiến API, Loader (Skaaa No-Code Design v1.2.3 & Skaaa Data Pro v1.3.2)
- **Decision (Early wp_head Pre-scan for Code Blocks):** Phát hiện và giải quyết xung đột vòng đời WordPress (block render trong body lỡ nhịp `wp_head`). Quyết định đăng ký hook `wp_head` độ ưu tiên 1 để tự động quét sớm toàn bộ block Code ngoài frontend (đệ quy cả inner blocks của Container và active theme organisms), đẩy CSS/JS inline vào hàng đợi và enqueue script thư viện trước khi `wp_head` render các thẻ style/script ở priority 100/101.
- **Decision (HTML Comment Script ID Debugging):** In thêm dòng chú thích HTML debug `<!-- Skaaa Script: [script_id] -->` trước mỗi script được nạp từ thư viện, hỗ trợ việc xác minh E2E và kiểm tra tính khử trùng lặp.
- **Decision (Skaaa Code Block Nesting):** Bổ sung block `skaaaaa-builder/code` vào danh sách `allowedBlocks` của `Skaaa Container` (`skaaa-container/index.js`). Quyết định này giúp người dùng chèn lồng block canvas/custom code vào bên trong container để dựng các card glassmorphism phức hợp chứa biểu đồ động Chart.js một cách linh hoạt.
- **Decision (Robust API Table Routing):** Cải tiến REST API portal (`class-rest-api.php`) tự động phân giải tên bảng từ request URL. Nếu không tìm thấy, nó sẽ thử tìm với prefix `skaaa_data_` (ví dụ: `revenue` -> `skaaa_data_revenue`) hoặc theo Portal Slug (ví dụ: `revenue-api`) và định tuyến lại tên bảng thật để MySQL query dữ liệu chính xác.
- **Decision (Duplicate Script Render Prevention):** Tối ưu hóa `Scripts_Loader` trong `Skaaa Data Pro` bằng cách thêm cơ chế theo dõi `self::$rendered_script_ids`. Tránh tình trạng một script trong Scripts Library bị in lặp chéo giữa `wp_head` (Header) và `wp_footer` (Footer) khi cả block và trang đều yêu cầu nạp script đó.
- **Decision (Block Icon Integration):** Đăng ký icon `code` từ `@wordpress/icons` cho block `Skaaa Code` (`skaaa-code/index.js`) để tăng tính thẩm mỹ và dễ nhận biết trên bảng Gutenberg Block Inserter.

## 2026-06-24 - 🟢 Hoàn thành: Triển khai block Gutenberg code (`skaaa/code`) và API thư viện script (Skaaa No-Code Design v1.2.0 & Skaaa Data Pro v1.3.0)
- **Decision (Decoupled Enqueue Hook):** Đăng ký action hook `skaaa_enqueue_custom_script` trong `class-scripts-loader.php` để block Gutenberg hoặc các plugin khác có thể gọi nạp script thư viện decoupled. Tuân thủ tuyệt đối chuẩn decoupled, tránh gọi chéo class.
- **Decision (REST API Scripts Management):** Cung cấp các endpoint `GET`/`POST` tại `/skaaa-data/v1/scripts` hỗ trợ Editor truy xuất danh sách và lưu trực tiếp script mới vào CSDL phẳng `wp_skaaa_data_sys_scripts` ngay trong giao diện làm việc của Gutenberg (nâng cao UX, không bị ngắt quãng).
- **Decision (Skaaa Code Block & Deduplication):** Triển khai block Gutenberg `skaaaaa-builder/code` với các tùy chọn nạp linh hoạt: Inline, Header, Footer. Nếu in ở Header/Footer, hệ thống băm MD5 nội dung code inline và quản lý hàng đợi `Skaaa_Code_Block_Queue` để đảm bảo mỗi đoạn code chỉ nạp 1 lần duy nhất ngoài frontend. Chỉ đăng ký block này khi `Skaaa Data Pro` được kích hoạt.

## 2026-06-23 - 🟢 Hoàn thành: Vá lỗi giao diện JIT Tailwind, Ẩn submenu sidebar & Sửa lỗi query danh sách scripts (Skaaa Data Pro v1.2.3)
- **Decision (Scripts Query Restored):** Phát hiện và vá lỗi logic thiếu truy vấn `$scripts = $wpdb->get_results(...)` ở phần đầu tệp `scripts.php`, khiến danh sách scripts luôn bị trống rỗng ngoài trang quản trị dù dữ liệu đã lưu trữ thành công dưới CSDL phẳng.
- **Decision (Tailwind PHP JIT Compilation & Native Fallback):** Tích hợp JIT Compiler của Skaaa No-Code Design qua filter hook 'skaaa_compile_tailwind' trên trang quản lý Scripts Library. Toàn bộ utility classes Tailwind được quét tĩnh ở server-side và compile thành CSS nhúng (offline-first), có fallback tự động về CDN nếu plugin Design bị tắt.
- **Decision (Hidden Admin Submenu):** Loại bỏ submenu 'Scripts Library' khỏi sidebar WordPress Admin để tránh làm rác danh mục theo yêu cầu. Giao diện hiện được truy cập gọn gàng và tập trung thông qua card Extensions ở Dashboard chính và các liên kết định tuyến nội bộ, route URL '?page=skaaa-data-pro-scripts' vẫn hoạt động bình thường nhờ đăng ký với parent slug là null. Nâng cấp phiên bản lên v1.2.3.

## 2026-06-17 - 🟢 Hoàn thành: Thiết lập SSH Key & Đóng gói phát hành bản chính thức v1.2.0
- **Decision (Git Push & Release Automation via SSH Key):** Tạo khóa SSH chuẩn Ed25519 mới (`~/.ssh/id_ed25519`) để giải quyết triệt để lỗi phân quyền SSH (`Permission denied (publickey)`) khi kết nối GitHub. Thực hiện merge toàn bộ thay đổi của phiên trước từ nhánh tính năng `feature/workspace-redirect-fallback` vào nhánh `main`, đẩy thành công lên nhánh chính GitHub và khởi chạy script `release.js` đóng gói, gắn tag Git `v1.2.0` chuẩn xác.

## 2026-06-15 - 🟢 Hoàn thành: Vá lỗi tự động đóng Sidebar Popup Menu & Asset Enqueue (Skaaa Data Pro v1.1.1)
- **Decision (Standardized Enqueue & Dependencies):** Nhúng script `admin-datagrid.bundle.js` thông qua hook `wp_enqueue_script` chuẩn của WordPress kèm theo các dependency `['wp-i18n', 'wp-util']` trong `Admin_Menu::enqueue_assets()`. Giải quyết triệt để lỗi `ReferenceError: wp is not defined` và sự cố các hàm callback modal (như `skaaaOpenDeleteApp`) không hoạt động do lỗi crash luồng JS trên các trang quản lý Data Sheet.
- **Decision (Click-Outside Dropdown Dimmer):** Triển khai event listener toàn cục xử lý đóng tự động tất cả các dropdown menu trong sidebar khi click ra ngoài. Sửa nhãn dropdown từ "Rename Space" thành "Workspace Settings" để đồng bộ với E2E testing guides.

## 2026-06-15 - 🟢 Hoàn thành: Chuẩn hóa lưu trữ Workspace & Redirect Fallback (Skaaa Data Pro & Skaaa No-Code Design v1.1.0)
- **Decision (Flat Table Workspace Storage):** Chuyển dịch toàn bộ cấu trúc lưu trữ App Workspace từ option `wp_options` (`skaaa_data_apps`) sang bảng phẳng hệ thống MySQL `wp_skaaa_data_sys_apps` trực thuộc plugin **Skaaa Data Pro**. Tránh việc sử dụng `wp_options` giúp tối ưu hóa hiệu năng truy vấn, lưu trữ thông tin Workspace tập trung và an toàn dưới dạng bảng hệ thống được bảo vệ qua `Database_Engine::is_table_protected()`.
- **Decision (App-Level Redirect Fallback & Custom 403 Page):** Triển khai cơ chế phân giải URL chuyển hướng khi bị chặn quyền truy cập (Access Denied) theo hai cấp: Table Portal -> Workspace Settings. Nếu cả 2 URL đều trống, hệ thống kích hoạt cờ `$skaaa_access_denied` để chuyển hướng sang location `403`. Cho phép người dùng thiết kế trang lỗi 403 bằng Skaaa Builder (display condition `is_403` hoặc location `403`), nếu chưa thiết kế thì sử dụng giao diện 403 default fallback tuyệt đẹp (OutFit font, Tailwind CSS, Glassmorphism).

## 2026-06-07 - 🟢 Hoàn thành: Khởi tạo Cơ chế Vòng lặp Học tập Liên tục (Continuous Learning Loop) & self-improve.md
- **Decision (Continuous Learning Loop Integration):** Khởi tạo tệp tin `.skaaa-ai/2-memory/self-improve.md` làm sổ tay giám sát lỗi hành vi của Agent. Tích hợp quy trình nạp bộ nhớ vào `start_session.md` và bước đánh giá tương tác (Interactive Session Review) vào `end_session.md` để ngăn chặn các lỗi lặp lại (như CLI mysql, thiếu i18n) và đảm bảo kiểm soát của User trước khi lưu lịch sử.

## 2026-06-07 - 🟡 Lên kế hoạch: Kiến trúc Community Nodes / Pluggable Nodes (Milestone 2+)
- **Decision (Community Nodes Architecture):** Thống nhất KHÔNG tích hợp tính năng viết code PHP/JS thô trực tiếp vào các node nguyên thủy (như `Set Data`, `Condition`) để bảo mật tuyệt đối hệ thống (tránh lỗi RCE), giữ core sạch và tuân thủ chặt chẽ triết lý No-code cho Power User. Toàn bộ logic tùy biến phức tạp (như Stripe, Twilio SMS, OpenAI...) sẽ được đóng gói thành các **Community Nodes (Pluggable Nodes)** độc lập. Lập trình viên phát triển module node này một lần, sau đó đăng ký vào builder để user kéo thả trực quan.

## 2026-06-07 - 🟢 Hoàn thành: Sửa lỗi hiển thị ký tự gạch dưới `_` trong Settings Panel (Skaaa No-Code Design v1.0.9)
- **Decision (Admin Font Specificity Isolation & admin_body_class):** Đã giải quyết triệt để lỗi ký tự gạch dưới bị tàng hình. Lý do CSS override trong `skaaa-editor-helper.js` chưa ăn là vì admin body chưa có class `skaaaaa-builder` để map đúng selector `body.skaaaaa-builder .interface-complementary-area input`. Bằng việc bổ sung filter `admin_body_class` trong file `class-core.php` để chèn class `skaaaaa-builder` vào body của WP Admin, style override đã hoạt động chuẩn xác. Font inputs trong sidebar được chuyển sang system font 14px và hiển thị dấu `_` cực kỳ rõ ràng. Nâng phiên bản `skaaa-no-code-design` lên `v1.0.9`.

## 2026-06-07 - 🟢 Hoàn thành: Đồng bộ hóa tên Workflow ID (Skaaa Logic Engine v1.2.6)
- **Decision (Workflow ID Alignment):** Sửa lỗi chính tả tên Workflow trong Database từ `test-skaaafx-autocomplate-node` thành `test-skaaafx-autocomplete-node` để đồng bộ với định nghĩa ở Frontend, sửa triệt để lỗi `Wrong Password: Logic id ... not found` gây ra bởi cơ chế validate bảo mật mới.

## 2026-06-07 - 🟢 Hoàn thành: Dọn dẹp và quy hoạch thư mục AI Workflows (.agent)
- **Decision (Agent Folder Consolidation):** Phát hiện và hợp nhất thư mục `.agents` (thư mục số nhiều, bị ignore bởi Git) vào `.agent` (thư mục số ít, được whitelist bởi Git). Di chuyển `debug-workflow.md` vào đúng cấu trúc `.agent/workflows/` và xóa bỏ thư mục `.agents` bị thừa để tuân thủ Zero-Trash Directive.

## 2026-06-07 - 🟡 Đang dở dang: Debug lỗi so sánh rẽ nhánh sai trong ConditionNode (v1.2.5)
- **Decision (Literal String Quotes Uniformity & Form Field Consistency):** 
  - Phân tích và phát hiện nguyên nhân 1: Biểu thức If/Else đánh giá `false` khi nhập nháy hỗn hợp dạng `[payload.render_template] == "'DISCOUNT50'`. Đã bổ sung cảnh báo vào `skaaafx-language.md`.
  - Phân tích và phát hiện nguyên nhân 2 (lý thuyết): Tên trường Form frontend không khớp với biến payload trong Render Template Node. Đã mô phỏng CLI thành công nhưng **User test trực tiếp trên browser vẫn không hoạt động**. Lỗi chưa được giải quyết triệt để — cần debug sâu hơn bằng `error_log()` hoặc DevTools để truy vết payload thực tế tại runtime.

## 2026-06-07 - 🟢 Hoàn thành: Vá lỗi Cắt khuất Dropdown Autocomplete & Thêm Fallback Biến Render Template (v1.2.5)
- **Decision (Dynamic Dropdown Positioning):** Triển khai cơ chế định vị động (above/below) cho dropdown gợi ý tự động của component `SkaaaFXInput` và `SkaaaFXTextarea`. Khi không gian trống bên dưới ô nhập liệu (spaceBelow) nhỏ hơn 220px, dropdown sẽ tự động mở lên phía trên (`bottom-full mb-1`) thay vì phía dưới (`top-full mt-1`), ngăn chặn hoàn toàn lỗi khuất hiển thị (viewport clipping) do thanh sidebar có thuộc tính `overflow-y-auto`.
- **Decision (Fallback Render Template Variable):** Hỗ trợ gợi ý song song cả hai dạng biến đầu ra `[payload.render_template]` và `[payload.rendered_template]` cho `RenderTemplateNode` nhằm tương thích tối đa với thói quen gõ và cấu hình của nhà phát triển.

## 2026-06-07 - 🟢 Hoàn thành: Vá lỗi Autocomplete Biến Đầu Ra và Tích Hợp Fallback Mock Payload Backend (v1.2.4)
- **Decision (Output Variables Auto-Fallback in Autocomplete):** Nâng cấp phương thức `getPrecedingResultVars` trong `SkaaaFXAutocomplete.jsx` để tự động gán giá trị mặc định cho biến đầu ra của `RenderTemplateNode` (`payload.rendered_template`) và `DBQueryNode` (`payload.query_results`) nếu người dùng chưa định nghĩa thủ công trong thiết lập node, đồng thời phân tích và trích xuất các biến gán từ `SetDataNode` assignments.
- **Decision (Mock Payload Nested 'payload' Backend Fallback):** Cải tiến bộ giải mã ngữ cảnh `SkaaaFX_Evaluator` trên PHP backend để tự động phát hiện và fallback truy cập dữ liệu bên trong khóa con `'payload'` (nếu tồn tại) của context khi chạy test trong Builder sandbox, giải quyết triệt để lỗi biên dịch trống của các thẻ template động (ví dụ: `{{payload.custom_coupon_code}}`) do cấu trúc bọc mock payload ở UI.

## 2026-06-06 - 🟢 Hoàn thành: Triển khai Giao diện Gợi ý Biến động & AST Autocomplete (v1.2.0)
- **Decision (Context-Aware Floating Autocomplete):** Triển khai một dropdown gợi ý biến động (Autocomplete) hiển thị trực quan ngay dưới các ô nhập liệu của SettingsPanel.jsx. Dropdown kích hoạt bằng phím `[` (cho biến/cột), `{` (cho template bindings `{{ }}`), hoặc chữ cái đầu (cho hàm built-in như IF, CONCAT).
- **Decision (Dynamic Context Parsing):** Tự động phân tích các key trong `mockPayload` JSON; quét đồ thị để trích xuất các biến đầu ra (`resultVar`, `result_var`) từ các node trước; và tự động phát hiện phân cấp cha-con của React Flow để hiển thị gợi ý biến vòng lặp `[$item]` và `[$index]` duy nhất khi node nằm trong `IteratorNode`.
- **Decision (Focus and Caret Restoring):** Sử dụng React refs để khôi phục tiêu điểm (focus) và di chuyển vị trí con trỏ chuột (caret) về đúng vị trí mong muốn (ví dụ: bên trong ngoặc hàm `IF( | )` hoặc sau dấu đóng ngoặc `]`) ngay sau khi người dùng click chọn biến, bảo đảm trải nghiệm UX liền mạch.

## 2026-06-05 - 🟡 Lên kế hoạch: Tách biệt và Đóng gói Package độc lập cho các Module cốt lõi
- **Decision (Tailwind JIT PHP & SkaaaFX / SkaaapineJS Packaging):** Thống nhất tầm nhìn dài hạn về việc đóng gói các cấu phần lõi có tính tái sử dụng cao (như cỗ máy Tailwind JIT Compiler viết bằng PHP thuần, ngôn ngữ biểu thức DSL SkaaaFX, và trình giả lập tương tác SkaaapineJS) thành các packages độc lập.
- **Decision (Distribution Pipeline via Composer & npm):** 
  - Tách cỗ máy Tailwind JIT PHP thành một repo riêng biệt (ví dụ: `tailwind-jit-php`), chuẩn hóa loại bỏ phụ thuộc WordPress để chạy được trên mọi framework PHP (Laravel, Symfony...). Xuất bản lên Packagist để cài đặt qua `composer require`.
  - Tách SkaaaFX và SkaaapineJS thành các npm packages (ví dụ: `@skaaaaa-builder/skaaafx`) xuất bản lên npm registry để tái sử dụng trong các frontend JS apps.
  - Tích hợp ngược lại vào Skaaa No-Code Ecosystem bằng cách khai báo dependency trong `composer.json` / `package.json` nhằm tinh giản mã nguồn core.

## 2026-06-03 - 🟢 Hoàn thành: Tích hợp JSON Blueprint Editor & Graph Switcher (v1.1.11)
- **Decision (Dual-Mode React Flow Canvas):** Triển khai cơ chế render có điều kiện (Conditional Rendering) trên component `DnDFlow` của Logic Builder. Hỗ trợ chuyển đổi nhanh giữa chế độ đồ thị (Graph View) và chế độ văn bản (JSON View) bằng một thanh công cụ Glassmorphism.
- **Decision (Safe JSON Modification & Syntax Validation):** Thiết lập một trình soạn thảo textarea chuyên dụng ở chế độ JSON View, hỗ trợ Copy nhanh bằng Clipboard API. Cho phép dán và chỉnh sửa trực tiếp, sau đó chạy qua khối `try...catch` để parse và kiểm tra cú pháp trước khi nạp lại vào React Flow. Nếu phát hiện lỗi cú pháp, hệ thống sẽ chặn lại và báo lỗi màu đỏ thay vì cập nhật state, triệt tiêu hoàn toàn rủi ro crash ứng dụng.

## 2026-06-03 - 🟢 Hoàn thành: Quét và JIT Compile Tailwind CSS từ Database động (v1.1.10)
- **Decision (Flat Table JIT Scan):** Khắc phục lỗi vỡ layout/thiếu class Tailwind trên Client Response Modal (Open Modal Popup) ở frontend. Vì nội dung modal được lấy động từ CSDL phẳng (`wp_skaaa_data_app_*`) qua Ajax, bộ biên dịch JIT PHP tĩnh lúc tải trang không thể phát hiện ra. Giải pháp là đăng ký filter `skaaa_design_classes_to_scan` trong `Skaaa_Logic_Core`, tự động truy xuất `skaaa_data_dictionary` để lọc ra tất cả các bảng phẳng của app và các cột có type là `long_text`/`rich_text`/`text`. Tiếp theo, query lấy toàn bộ giá trị không rỗng từ các cột này, chạy regex trích xuất class Tailwind, và lưu cache transient `skaaa_dynamic_tailwind_classes_cache` trong 12 giờ.
- **Decision (Proactive Cache Invalidation):** Thiết lập việc tự động xoá transient cache `skaaa_dynamic_tailwind_classes_cache` bất cứ khi nào có hành động thay đổi CSDL (Insert/Update/Delete thành công thông qua `Skaaa_Logic_DB_Action`) hoặc khi người dùng lưu/cập nhật workflow trong Admin, đảm bảo JIT stylesheet luôn được cập nhật chính xác với chi phí tải trang tối ưu (0ms overhead do đọc thẳng transient).

## 2026-06-03 - 🟢 Hoàn thành: Thiết lập Shadow Scratchpad Full-Screen (v1.0.6)
- **Decision (Full-Screen Gutenberg Design Modal):** Loại bỏ hành vi hiển thị Scratchpad Modal dạng cửa sổ pop-up thu nhỏ làm che khuất và thu hẹp thiết kế (gây khó khăn cho việc review). Cải tiến CSS và HTML của `.skaaa-designer-modal-overlay` (đặt padding về 0) và `.skaaa-designer-modal-container` (đặt max-width/max-height thành 100% / 100vh, gỡ bỏ bo tròn góc và border). Đảm bảo giao diện editor Gutenberg nhúng trong Iframe chiếm trọn 100% viewport màn hình thiết bị giúp người dùng kiểm tra thiết kế trực quan ở độ phân giải thực tế.

## 2026-06-03 - 🟢 Hoàn thành: Tích hợp Dynamic Modal cho Client Response Node (v1.1.7)
- **Decision (Dynamic Client-Side Modal & HTML Preservation):** Loại bỏ bắt buộc phải chỉ định `modal_id` tĩnh khi chọn Response Type là `open_modal`. Cho phép truyền trực tiếp nội dung HTML đã được render động (`modal_content`) từ Backend xuống Client. Tại Frontend (`skaaa-core.js`), khi sự kiện `open_modal` kích hoạt, nếu có `modal_content` thì JS sẽ tự động render một Dynamic Modal Container có overlay mờ (`backdrop-filter`), floating close button, ESC/click-outside handler, và chèn nội dung HTML thô vào trong để bảo toàn 100% thiết kế Tailwind gốc mà không bị ép style.
- **Decision (Wide Sidebar for Client Response):** Nới rộng Sidebar Settings React của `ClientResponseNode` lên `w-[400px]` (tương tự RenderTemplateNode) để cung cấp không gian rộng rãi cho việc nhập code/biến SkaaaFX trong trường `modal_content`.

## 2026-06-03 - 🟢 Hoàn thành: Tích hợp Pure Render Template Node & Giao diện Live Testing & Preview (v1.1.6)
- **Decision (Pure Render Architecture & Two-Pass Interpolation):** Xóa bỏ hoàn toàn mã nguồn truy vấn SQL tới bảng `skaaa_data_sys_organisms` trong `Skaaa_Logic_Render_Template`. Chuyển đổi thành Pure Render Node, thực thi bộ nội suy 2 bước (Two-Pass Interpolation) tuần tự: giải quyết biến chứa HTML thô từ payload trước, sau đó nội suy các biến dữ liệu con bên trong HTML đó. Đảm bảo tính tương thích ngược cho dữ liệu cũ (`raw_template` và `organism_id` sẽ tự động map về `template_html`). Đồng thời, tích hợp hàm `do_blocks()` của WordPress để tự động biên dịch Gutenberg Block Markup thành HTML thật trước khi lưu/trả kết quả, cho phép render động các Skaaa Blocks trực quan ở Frontend.
- **Decision (Reactive Sidebar Width Adjustment):** Nâng cao UX bằng cách thiết lập sidebar mở rộng tự động lên `w-[400px]` (với hiệu ứng transition mượt mà) khi chọn các node đòi hỏi vùng nhập code lớn như `RenderTemplateNode` và `ApiNode`. Các node thông thường vẫn giữ nguyên `w-80`.
- **Decision (Glassmorphic Testing Sandbox):** Tích hợp sandbox Live Testing & Preview trực tiếp bên trong Settings Panel của RenderTemplateNode. Sandbox cho phép người dùng điền JSON Payload giả lập, tự động phát hiện và hiển thị lỗi cú pháp JSON và hỗ trợ chuyển đổi tab giữa Visual Preview (Render HTML) và Raw HTML Output giúp người dùng debug nhanh mà không cần deploy workflow thật.

## 2026-07-09 - 🟢 Hoàn thành: Tích hợp hệ thống Pluggable Nodes và Extensions Manager trên Dashboard (Logic Engine v1.3.0 & No-Code Design v2.1.0)
- **Decision (Flat Table Settings):** Tạo bảng phẳng hệ thống `wp_skaaa_data_sys_settings` để lưu cấu hình thay vì dùng `wp_options` của WordPress. Viết 2 hàm helper toàn cục `skaaa_get_system_setting()` và `skaaa_set_system_setting()` để đọc/ghi dễ dàng trên toàn hệ sinh thái.
- **Decision (Node Soft-Toggle):** Tránh can thiệp bật/tắt vật lý plugin WordPress khi người dùng toggle trên Dashboard (để giữ hệ thống hoạt động ổn định và bảo mật). Thay vào đó sử dụng mảng `skaaa_disabled_nodes` lưu trong bảng settings phẳng để vô hiệu hóa mềm node trên Logic Canvas (mờ đi, cấm kéo thả).
- **Decision (Dynamic File Scan Fallback for Uninstall):** Khi người dùng xóa vật lý plugin addon từ Dashboard, hệ thống định vị plugin file qua Reflection Class. Nếu class chưa được load hoặc định nghĩa thực tế (node ảo/node mockup), hệ thống tự động chuyển sang cơ chế quét nhanh nội dung tệp chính của tất cả plugin `skaaa-` để tìm node_type tương ứng, nâng cao độ tin cậy và tự động hóa 100%.
- **Decision (Dynamic Form Validation & React Error Fallback):** Thêm cơ chế fallback an toàn về icon mặc định `ServerCog` trong sidebar React khi bên thứ ba định nghĩa icon không tồn tại hoặc lỗi trong lucide-react, tránh crash làm trắng trang Editor.

## 2026-06-03 - 🟡 Lên kế hoạch: Triển khai Gutenberg Wrapper Filter JS để đồng bộ Flex/Grid Layout
- **Decision (Gutenberg Wrapper Filter Hook):** Để khắc phục triệt để tình trạng các thẻ bọc Gutenberg (`.wp-block` wrapper) làm đứt gãy cấu trúc Flexbox/Grid và vô hiệu hóa các class sắp xếp (`order-*`, `col-span-*`, `row-span-*`, `self-*`) trong Editor, chúng ta sẽ triển khai bộ lọc JS React sử dụng hook `editor.BlockListBlock`. Bộ lọc này sẽ tự động phát hiện các class định vị từ attributes của block Skaaa và bơm trực tiếp lên thẻ wrapper của Gutenberg. Giải pháp này giúp hiển thị WYSIWYG đồng bộ 100% với Frontend mà không làm ảnh hưởng đến mã nguồn HTML sạch lưu trong Database.

## 2026-06-01 - 🟡 Lên kế hoạch: Tách biệt Client Response (Milestone 2+) & Pure Render Node (Milestone 1)
- **Decision (Community Nodes / User Nodes - Hướng A - Microservices Architecture):** Thống nhất mô hình tách biệt các hành động của Client Response và Trigger thành các **Node con độc lập trên Sidebar (User Nodes / Community Nodes)** theo triết lý Microservices/Plugin-pack. Các Action/Trigger lúc này được đóng gói như một **Module độc lập có thể tự do cài vào hoặc gỡ ra (Pluggable)**. Mỗi Module sẽ gồm:
  1. File JSON/Metadata mô tả Schema (các trường cấu hình để Gutenberg tự vẽ Form Settings động).
  2. File JS/PHP độc lập chứa logic xử lý nghiệp vụ (Ví dụ: Stripe Trigger, Confetti Response, SMS Sender, v.v.).
  Cách tiếp cận này giúp loại bỏ hoàn toàn mã nguồn switch-case cồng kềnh trong core, biến Skaaa Logic Engine thành một Runtime nền tảng (Execution Platform) và mở ra cơ hội thương mại hóa cực cao thông qua việc bán các gói Action/Trigger. Hạng mục này rất lớn nên sẽ được dời sang **Milestone 2+**. Trong Milestone 1, các action vẫn được xử lý tập trung trong core để hoàn thành gấp các kịch bản Pop-up và Box khóa nội dung.
- **Decision (Pure Render Template Node):** Thống nhất lựa chọn Cách 2 (Pure Render Node). Node Render Template chỉ chịu trách nhiệm render HTML thô bằng cách chạy bộ nội suy 2 bước (Two-Pass Interpolation: bước 1 phân giải biến chứa HTML template động từ DB Query, bước 2 nội suy dữ liệu cá nhân hóa bên trong nó). Node này hoàn toàn không trực tiếp kết nối hay truy vấn CSDL.

## 2026-06-01 - 🟡 Lên kế hoạch (Post-Milestone 1): Cải tiến UX/UI Quản lý Thư mục Workflows
- **Decision (Workflow Portability):** Lên kế hoạch cho phép người dùng di chuyển các Workflows linh hoạt giữa các Folder/App khác nhau. Thiết kế sẽ bổ sung cột `app_id` vào cài đặt cấu hình workflow, hiển thị nút "Settings / Move" trực quan trên bảng quản trị giúp cập nhật nhanh liên kết App trong CSDL.
- **Decision (Folder Inline Rename):** Cho phép đổi tên trực tiếp các App Workspace (Thư mục) từ Logic Manager UI. Giao diện sẽ hiển thị icon chỉnh sửa ✏️ bên cạnh tên thư mục và tự động đồng bộ hóa dữ liệu cập nhật tên App sang cấu hình Data Pro (`skaaa_data_apps`), bảo toàn nguyên lý Single Source of Truth.

## 2026-06-01 - 🟢 Hoàn thành: Tích hợp AI JSON Blueprint Import / Export cho Skaaa Logic Engine (v1.1.5)
- **Decision (Blueprint API Endpoint):** Xây dựng lớp `Skaaa_Blueprint_API` độc lập và đăng ký 2 REST endpoint `POST /skaaa-logic/v1/import-blueprint` và `GET /skaaa-logic/v1/export-blueprint` (Yêu cầu quyền `manage_options`). Cho phép giao tiếp trực tiếp Machine-to-Machine, giúp AI Agent có thể nạp thẳng file JSON chứa chuỗi Workflow phức tạp (Nodes & Edges) vào CSDL phẳng `wp_skaaa_data_sys_workflows`.
- **Decision (REST API Authentication Nonce):** Khi truy cập trực tiếp API Export qua link download trên trình duyệt (`target="_blank"`), WordPress REST API sẽ không nhận diện Cookie phiên đăng nhập nếu thiếu REST Nonce. Giải quyết bằng cách tạo và đính kèm tham số `_wpnonce` (`wp_create_nonce('wp_rest')`) trực tiếp vào query string để xác thực quyền quản trị thành công.
- **Decision (Manager UI File I/O):** Thay vì copy-paste thủ công gây rủi ro sai cú pháp, hệ thống được thiết kế đồng bộ với cơ chế File I/O: tính năng Export tải trực tiếp định dạng `.json` về máy, và tính năng Import sử dụng Form Upload `multipart/form-data` (`<input type="file">`).
- **Decision (Safe Overwrite):** Tích hợp cờ bảo vệ `overwrite`. Quá trình import sẽ kiểm tra sự tồn tại của `workflow_id` và chỉ cho phép cập nhật đè lên Workflow cũ nếu người dùng cố ý check vào hộp kiểm `Overwrite if exists`, tránh rủi ro phá hỏng luồng automation đang chạy.
- **Decision (Admin UI Responsiveness):** Khắc phục tình trạng layout hiển thị hẹp (900px) gây xuống dòng và chồng chéo các nút hành động (Design Flow, Export, Delete) ở cột Operations. Thiết lập `max-width: 1200px;` cho Wrapper chính, điều chỉnh tỷ lệ cột `<thead>` (45% | 15% | 40%), đồng thời gói các nút Operations vào một flexbox container (`justify-content: flex-end`) cùng với quy tắc `white-space: nowrap` và chuyển dịch các nhãn nút sang tiếng Anh bọc i18n chuẩn.

## 2026-06-01 - 🟢 Hoàn thành: Triển khai tính năng Phân loại Skaaa Organisms & Quản lý Thư mục ảo (Skaaa No-Code Design v1.0.4 & Skaaa Data Pro v1.0.5)
- **Decision (Flat Table Category Column):** Bổ sung cột `category` (`varchar(255) DEFAULT NULL`) vào bảng phẳng `wp_skaaa_data_sys_organisms` thay vì dùng taxonomy hay postmeta để đảm bảo hiệu năng và tính độc lập. Đăng ký thuộc tính này trong Data Dictionary để đồng nhất schema.
- **Decision (Workspace Sidebar & Count Badges):** Xây dựng Sidebar quản lý danh mục (Categories) trên Workspace Panel của Design Engine. Hỗ trợ CRUD danh mục thông qua REST API. Đếm số lượng symbols theo từng danh mục bằng query SQL tối ưu và cập nhật reactive trên badge Sidebar thông qua Alpine.js.
- **Decision (Safe Cascading Category Deletion):** Khi người dùng xóa một danh mục custom, hệ thống tự động gán lại thuộc tính category của các symbols con thuộc danh mục đó về giá trị trống (NULL) để đưa chúng trở lại nhóm "Uncategorized", đảm bảo symbols không bị xóa mất dữ liệu ngoài ý muốn.
- **Decision (Physical Cache & window.skaaaOrganismsCache Auto-Sync):** Thực hiện tự động cập nhật file cache JSON vật lý `organisms-cache.json` và cập nhật biến cache toàn cục của trình duyệt `window.skaaaOrganismsCache` ngay khi có bất kỳ hành động đổi danh mục hoặc CRUD danh mục nào xảy ra.
- **Decision (Gutenberg Selector Optgroup):** Nâng cấp logic render danh sách symbol trong react block selector (`edit.js` của block Organism Ref). Chuyển đổi dropdown select phẳng thành dạng có phân nhóm `<optgroup>` dựa trên thuộc tính `category` của các symbol trong cache để nâng cao trải nghiệm quản trị.

## 2026-05-30 - 🟢 Hoàn thành: Vá lỗi block select liên kết động trên cả Editor và Frontend (Skaaa No-Code Design v1.0.3 & Skaaa Logic Engine v1.1.2)
- **Decision (FieldName Dynamic Connection Fallback):** Cải tiến logic trong `render.php` của block select để trích xuất `fieldName` trực tiếp từ biểu thức `skaaaDynamicBinding` (regex match `{{#foreach table.field}}` -> `field`) nếu `fieldName` trống hoặc trùng với giá trị mặc định `'my_select'`. Lỗi xảy ra do Gutenberg không lưu `fieldName` khi liên kết động, dẫn đến nó lấy giá trị mặc định của `block.json` và làm gãy Alpine form model binding.
- **Decision (Checkbox & Radio List Dynamic Templating):** Sửa đổi phương thức `Skaaa_Dynamic_Content::process_foreach_loop()` để tự động phát hiện và trích xuất cấu trúc thẻ `<label>` thô làm template cho các block select hiển thị ở chế độ checkbox hoặc radio (thay vì mặc định nạp thẻ `<option>` sai cấu trúc HTML). Hệ thống sẽ tự động thay thế thuộc tính `value` và nhãn `<span>` động tương ứng với dữ liệu CSDL.
- **Decision (Form Init Lifecycle Race Condition Fix):** Gỡ bỏ lệnh trì hoãn `$nextTick` trong hàm `init()` của `skaaaForm` (file `skaaa-frontend.js`) để khởi tạo các thuộc tính form đồng bộ trước khi Alpine biên dịch DOM. Điều này ngăn chặn race condition khiến các checkbox liên kết động bị Alpine coi là boolean checkbox và xóa sạch giá trị `value` trên DOM.
- **Decision (JSX i18n Curly Braces Fix):** Bọc các hàm i18n `__()` trong dấu ngoặc nhọn `{}` cho các thuộc tính component (như `placeholder`, `help`, `label`, `instructions`) phân tán trên 10 file nguồn JSX để sửa triệt để các lỗi biên dịch của Webpack.

## 2026-05-30 - 🟢 Hoàn thành: Vá lỗi lưu CSDL JSON khi sửa ô trực tiếp trên lưới (Skaaa Data Pro v1.0.4)
- **Decision (Inline Cell Update Normalization):** Tích hợp bộ chuẩn hóa dữ liệu JSON vào phương thức `Database_Engine::update_cell()`. Khi người dùng sửa đổi giá trị ô trực tiếp trên lưới DataGrid (gửi qua AJAX `wp_ajax_skaaa_data_update_cell`), nếu cột sửa đổi thuộc kiểu JSON vật lý (`multi_select`, `relation`, `rollup`), giá trị thô nhận từ frontend sẽ được chuẩn hóa thành `NULL` hoặc định dạng JSON Array hợp lệ trước khi gọi lệnh cập nhật của WordPress, triệt tiêu lỗi `Invalid JSON text` khi người dùng nhập tay các giá trị thô.

## 2026-05-30 - 🟢 Hoàn thành: Khắc phục lỗi lưu CSDL JSON khi đệ trình mảng rỗng hoặc CSV thô (Skaaa Data Pro v1.0.3 & Skaaa Logic Engine v1.1.1)
- **Decision (MySQL JSON Column Null Cast):** Thiết lập cơ chế tự động chuyển đổi các trường dữ liệu thuộc kiểu JSON vật lý (`multi_select`, `relation`, `rollup`) sang giá trị `NULL` trong MySQL nếu dữ liệu đầu vào trống (empty string `""`, `null`), thay vì cố gắng lưu chuỗi rỗng `""` dẫn đến lỗi `Invalid JSON text` từ phía MySQL.
- **Decision (Standardized Relation Array Storage):** Định hình lại việc lưu trữ các cột quan hệ (`relation`) dưới dạng mảng JSON chứa các số nguyên ID (ví dụ: `[1, 2, 3]`) thay vì lưu chuỗi CSV thô (ví dụ: `'1,2,3'`), tuân thủ chuẩn kiến trúc Native JSON gốc.
- **Decision (Robust Dictionary Fallbacks):** Cải tiến bộ lọc chắt lọc dữ liệu của Skaaa Data Pro (`skaaa-data-pro.php`) để tìm cấu hình bảng trong dictionary qua nhiều định dạng khóa khác nhau (có/không có tiền tố `wp_` hoặc tên bảng thô), ngăn chặn các trường hợp lookup bị trả về mảng rỗng khiến hệ thống bỏ qua việc xác định cột JSON.
- **Decision (Universal JSON Parse Fallback):** Cập nhật hàm `Data_Fetcher::enrich_relations()` hỗ trợ giải mã cả định dạng JSON Array chứa các ID số nguyên/đối tượng lẫn chuỗi CSV thô để bảo toàn tính tương thích ngược cho dữ liệu cũ.

## 2026-05-30 - 🟢 Hoàn thành: Vá lỗi block validation khi sinh Portal App (Skaaa No-Code Design v1.0.1)
- **Decision (Gutenberg Block Comment Fix):** Khắc phục lỗi sinh sai cú pháp comment block của Gutenberg (sử dụng `//->` và `//>` thay vì ` /-->`) trong Portal Generator. Chuyển sang định dạng comment sạch, trích xuất chuỗi dịch động bọc hàm `__()` thay vì bọc toàn bộ khối comment.
- **Decision (MySQL Data Migration & Repair):** Viết và chạy tập lệnh sửa lỗi cơ sở dữ liệu `repair_database.php` quét bảng phẳng `skaaa_data_sys_organisms`, tìm các comment block lỗi, sửa định dạng HTML chuẩn và lưu lại để khôi phục hoạt động của Gutenberg Editor ngay lập tức.

## 2026-05-30 - 🟢 Hoàn thành: Dịch toàn bộ chuỗi tiếng Việt thô trong Skaaa Data Pro sang tiếng Anh bọc i18n và đổi "XACNHAN" thành "CONFIRM" (v1.0.2)
- **Decision (Strict i18n Compliance):** Dịch toàn bộ các chuỗi giao diện tiếng Việt thô hiển thị trên UI Data Grid (`manage.php`), Sidebar (`manage-sidebar.php`), Modals (`manage-modals.php`) và Dashboard (`dashboard.php`) sang tiếng Anh và bọc trong hàm dịch chuẩn của WP (`esc_html_e`, `__`, `_n`).
- **Decision (Consistent Confirmation Keyword):** Thay đổi từ khóa xác nhận thao tác phá hủy (Xóa bảng, Xóa App) từ tiếng Việt cứng "XACNHAN" sang tiếng Anh "CONFIRM" trên cả giao diện HTML/PHP và logic xử lý JS (`apps.js`, `schema.js` và bundle).

## 2026-05-28 - 🟢 Hoàn thành: Triển khai bảo vệ cấu trúc bảng hệ thống cốt lõi (Skaaa Data Pro v1.0.1 - Approach A)
- **Decision (Unified Protect Helper):** Bổ sung phương thức `Database_Engine::is_table_protected( $table_name )` thực hiện kiểm tra động qua filter `skaaa_data_protected_tables`. Hàm này được gọi chọc chặn đứng mọi thay đổi (thêm/sửa/xóa cột, đổi tên, xóa bảng) tại backend.
- **Decision (Locked Grid UI & Padlock Icon):** Cập nhật giao diện grid view (`manage.php`) thay thế nút thêm cột `[+]` bằng biểu tượng ổ khóa xám kèm chú giải khi bảng thuộc diện bảo vệ. Ẩn toàn bộ icon edit/dropdown cấu hình cột để khóa cứng cấu hình.
- **Decision (Hidden Sidebar Table Settings):** Cập nhật `manage-sidebar.php` tự động ẩn nút ellipsis (kebab menu) ở sidebar đối với bảng hệ thống để ngăn người dùng mở hộp thoại đổi tên/xóa bảng.

## 2026-05-28 - 🟢 Hoàn thành: Tái cấu trúc cơ sở dữ liệu lưu trữ Workflows sang bảng phẳng MySQL (v1.1.0)
- **Decision (System Grouping & Protection):** Đổi tên bảng phẳng thành `wp_skaaa_data_sys_workflows` để liên kết đồng bộ vào nhóm Workspace hệ thống `Site Management` (`skaaa_system`). Triển khai WP Filter `skaaa_data_protected_tables` trong plugin **Skaaa Data Pro** và hook từ phía **Skaaa Logic Engine** để bảo vệ bảng cấm xóa.
- **Decision (Dev Mode & Hybrid Routing):** Tích hợp cờ Dev Mode `skaaa_system_dev_mode` của hệ sinh thái:
  - Dev Mode Bật (1): Query MySQL trực tiếp lấy danh sách ID Workflow để đăng ký admin route ẩn ngầm.
  - Dev Mode Tắt (0): Đọc từ cache option `skaaa_logic_workflow_ids` giúp nâng tốc độ lên 0ms và loại bỏ hoàn toàn query SQL trên mỗi lượt tải trang admin.
- **Decision (Lazy Loading & Node Count):** Cập nhật Manager UI chỉ select các thông tin chung (Lazy loading cột `graph` dạng JSON) và hiển thị phân nhóm theo App Workspace. Cập nhật cột node_count tự động đếm số node khi lưu đồ thị.

## 2026-05-26 - 🟢 Hoàn thành: Dọn dẹp tài liệu Hệ thống (System Map) & Cập nhật Backlog Node Render HTML (Milestone 1)
- **Decision (Condensed System Map):** Rút gọn tệp `system_map.md` từ 167 dòng xuống cấu trúc tối giản và cô đọng. Tổng hợp toàn bộ thành tựu của Phase 3 & 4 thành một phần Checkpoint chuyên biệt và súc tích để tiện theo dõi.
- **Decision (Backlog Entry for Render HTML Node):** Chính thức ghi nhận hạng mục nâng cấp giao diện cấu hình trực quan và live preview cho Render HTML (Render Template Node) vào tệp nháp `project_manager_post_mvp_backlog.md` để triển khai trong Milestone 1.

## 2026-05-26 - 🟢 Hoàn thành: Tích hợp quy trình Git Branch, Đánh dấu phiên bản SemVer và Chuẩn hóa Bộ quy tắc AI
- **Decision (Git Branch Workflow Integration):** Thống nhất và tích hợp quy trình làm việc Git Branch vào vòng đời phát triển của Skaaa Ecosystem. Sử dụng nhánh `feature/refactor-logic-db` để bảo vệ nhánh `main` và cô lập các chỉnh sửa cơ sở dữ liệu Logic Engine.
- **Decision (Standardized Versioning Rule):** Bổ sung mục quy chuẩn **VERSIONING RULES** vào tệp `.agent/rules/skaaa-nocode-system.md` để ràng buộc các AI Agent phải tự động tăng số phiên bản của Plugin/Theme (theo chuẩn SemVer: MAJOR.MINOR.PATCH) trong các file định nghĩa chính (`Version: X.Y.Z` trong code header PHP/style.css/json) khi sửa đổi, và đồng bộ hóa Recent Logs.
- **Decision (Unified Handoff via Checkpoint.md):** Khôi phục tệp bàn giao tiến độ `checkpoint.md` và tinh chỉnh quy tắc `.agent/rules/skaaa-docs-management.md` để chính thức hóa tệp này dưới dạng 1 trong 5 điểm chạm được phép sửa đổi khi kết thúc phiên. Tránh lưu trữ trạng thái phiên ở system_map.md để bảo toàn tính độc lập của bản đồ hệ thống.
- **Decision (Marketing Workspace Rules Cleanup):** Phát hiện và di chuyển tệp `token-saving-rules.md` (thuộc dự án Skaaa Marketing dùng Obsidian) ra khỏi thư mục quy tắc của dự án code hiện tại sang `/home/chiconcota/Downloads/marketing-ideas/` để trả lại sự gọn gàng cho dự án.

## 2026-05-25 - 🟢 Hoàn thành: Khảo sát kiến trúc Blueprint & Lên kế hoạch Refactor Logic Engine Storage (Phase 4.6)
- **Decision (MySQL Database for Workflows):** Quyết định loại bỏ việc lưu trữ đồ thị Workflows trong `wp_options` của WordPress. Thay vào đó, thiết kế bảng phẳng MySQL chuyên biệt mang tên `skaaa_logic_workflows` (chứa các cột: `id`, `workflow_id`, `name`, `status`, `graph` [JSON], `updated_at`).
- **Decision (Isolated API Query):** Sửa đổi cơ chế đọc/ghi của `Skaaa_Logic_Core` và `Skaaa_Workflow_Runner` để query riêng lẻ từng bản ghi theo `workflow_id` thay vì load/save nguyên mảng PHP Array serialized từ options, giải quyết triệt để nguy cơ Autoload Bloat và Race Condition khi nhiều admin lưu dữ liệu cùng lúc.
- **Decision (Database Migration Script):** Xây dựng module tự động chuyển đổi dữ liệu khi kích hoạt phiên bản mới, đọc option `skaaa_logic_simple_workflows` cũ, bóc tách và chèn vào bảng phẳng mới, sau đó xóa option cũ để dọn dẹp hệ thống sạch sẽ.
- **Decision (Data Pro & Design Storage Verification):** Xác nhận dữ liệu Organisms (UI Blocks), Theme Templates, Presets (Skaaa No-Code Design) và dữ liệu Smart Object (Skaaa Data Pro) đã lưu trữ 100% bằng bảng phẳng MySQL, đảm bảo tính tối ưu hiệu năng và an toàn cao.

## 2026-05-24 - 🟢 Hoàn thành: Tách biệt mã nguồn Skaaa Ecosystem ra khỏi WordPress Core (Open-source Preparation)
- **Decision (Strict Git Ignore Rules):** Áp dụng quy tắc ignore triệt để trong `.gitignore` (`/*`) và whitelist chọn lọc (`!/wp-content/plugins/skaaa-*`, `!/wp-content/themes/skaaa-canvas/`, và các file tài liệu `.agent/`, `.skaaa-ai/`, `README.md`, `LICENSE`, `CONTRIBUTING.md`, `docs/`).
- **Decision (Git Index Cleaning):** Dọn dẹp Git cache index bằng cách chạy `git rm -r --cached .` để gỡ bỏ toàn bộ tệp tin WordPress Core và cấu hình LocalWP khỏi Git tracking mà không làm ảnh hưởng đến các tệp tin vật lý thực tế trên máy cục bộ của nhà phát triển.
- **Decision (Roadmap Synchronization):** Cập nhật file `project_manager_opensource.md` để thay thế tên thư mục theme Canvas cũ (`skaaa-blank-theme` thành `skaaa-canvas`) và đánh dấu hoàn thành nhiệm vụ tách biệt dự án (Task 5).

## 2026-05-24 - 🟢 Hoàn thành: Rà quét bảo mật và Làm sạch lịch sử Git (Open-source Preparation)
- **Decision (API Keys Verification):** Thực hiện rà quét mã nguồn và xác nhận không có API Key nhạy cảm nào bị hardcode. API Key của Gemini được quản lý an toàn qua Options API của WordPress (`skaaa_gemini_api_key`), đảm bảo tính bảo mật khi công khai mã nguồn.
- **Decision (Git History Cleaning):** Sử dụng công cụ `git-filter-repo` để xóa vĩnh viễn các tệp tin tạm và nhạy cảm khỏi toàn bộ lịch sử commit, bao gồm: `wp-content/debug.log`, `skaaa-no-code-design.zip`, các file `diff.txt`, `test_output.txt`, và các file zip/log cũ khác làm phình dung lượng.
- **Decision (Git Garbage Collection):** Thực thi dọn rác Git tối ưu (`git gc --prune=now --aggressive`) sau khi viết lại lịch sử để giải phóng hoàn toàn dung lượng lưu trữ cục bộ của repository.

## 2026-05-24 - 🟢 Hoàn thành: Kết nối GitHub bằng SSH Key và Chuẩn hóa Git Line Endings
- **Decision (Establish SSH Connection):** Do GitHub đã chính thức khai tử xác thực bằng mật khẩu tài khoản trực tiếp qua giao thức HTTPS (báo lỗi `Password authentication is not supported`), hệ thống chuyển đổi hoàn toàn địa chỉ remote sang giao thức SSH (`git@github.com:chiconcota/skaaa-nocode-ecosystem.git`).
- **Decision (SSH Key Pair Generation):** Tạo mới một cặp khóa SSH an toàn theo chuẩn Ed25519 (`id_ed25519`) và hỗ trợ người dùng tích hợp Public Key vào cấu hình tài khoản GitHub cá nhân để xác thực không cần mật khẩu.
- **Decision (Git User Identity Configuration):** Thiết lập cấu hình toàn cục `user.name` là `chiconcota` và `user.email` là `lytatthanh@gmail.com` dựa trên thông tin lịch sử commit để đảm bảo tính nhất quán của dữ liệu đóng góp (contributions).
- **Decision (Local Line Endings Normalization):** Cấu hình `core.autocrlf` thành `input` để thích ứng với môi trường điều hành Linux mới. Thực hiện làm sạch và khôi phục chỉ mục cache (`git rm --cached` và `git reset --hard`) để giải quyết triệt để lỗi Git báo đổi dòng (CRLF sang LF) trên toàn bộ dự án, đưa thư mục làm việc về trạng thái sạch sẽ.
- **Decision (Clean Credential Helper):** Gỡ bỏ cấu hình `credential.helper store` và tệp chứa thông tin mật khẩu tạm thời `~/.git-credentials` để bảo vệ an toàn bảo mật, tránh rò rỉ thông tin cá nhân.
